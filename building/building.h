#pragma once

#include "utilities.h"

class Building {
public:
    Building() = default;
    ~Building() = default;

    static void draw();

    static void draw_window(float x, float y, float z, int lado);
private:

};
